import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/**
 * Prompts help users discover what can be done with the server, and how to use it. 
 * They can be customized with vars, etc.
 * Think of them as like /commands in cursor - like autocomplets, or conversation starters, or request templates that help
 * users understand how to use the MCP tool
 */
export function registerPrompts(server: McpServer): void {
    
}

